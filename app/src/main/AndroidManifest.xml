<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ctatum.collegedatabase">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Latest template for database apps requires a name attribute for application tag -->

    <application
        android:name=".CollegeDataApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        >
        <activity
            android:name=".datalist.ListCollegesActivity"
            android:exported="true" />
        <activity
            android:name=".splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".home.HomeActivity"
            android:exported="true"></activity>
        <activity
            android:name=".edit.EditCollegeActivity"
            android:exported="true"></activity>
    </application>

</manifest>